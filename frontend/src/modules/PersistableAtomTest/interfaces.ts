type SettingsToViewInterface = {
    simpleNumber: number;
    upstream: number;
    downstream: number;
    precomputed: number;
};

export type Interfaces = {
    settingsToView: SettingsToViewInterface;
};
