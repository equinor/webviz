
export enum QueryStatus {
    Loading = "Loading",
    Error = "Error",
    Idle = "Idle",
}

export enum VfpParam {
    THP = "THP",
    WFR = "WFR",
    GFR = "GFR",
    ALQ = "ALQ",
}

export enum PressureOption {
    BHP = "BHP",
    DP = "DP",
}
