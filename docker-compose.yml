# This configuration aims at being a good setup
# for doing development of the frontend + backend in combination.

services:
  frontend:
    build:
      context: .
      dockerfile: frontend-dev.Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./frontend/public:/usr/src/app/frontend/public
      - ./frontend/src:/usr/src/app/frontend/src
      - ./frontend/theme:/usr/src/app/frontend/theme
      - ./frontend/index.html:/usr/src/app/frontend/index.html

  backend-primary:
    build:
      context: .
      dockerfile: ./backend_py/primary/Dockerfile
    ports:
      - 5000:5000
      - 5678:5678
    environment:
      - UVICORN_PORT=5000
      - UVICORN_RELOAD=true
      - UVICORN_RELOAD_DIRS=/home/appuser/backend_py
      - WEBVIZ_CLIENT_SECRET
      - WEBVIZ_SMDA_SUBSCRIPTION_KEY
      - WEBVIZ_ENTERPRISE_SUBSCRIPTION_KEY
      - WEBVIZ_SUMO_ENV
      - WEBVIZ_VDS_HOST_ADDRESS
      - WEBVIZ_PSEUDONYM_HMAC_KEY
      - WEBVIZ_SESSION_STORE_FERNET_KEY
      - WEBVIZ_REDIS_AUTH_STORE_PASSWORD
      - WEBVIZ_REDIS_CACHE_PASSWORD
      - WEBVIZ_SERVICE_BUS_FQ_NAMESPACE
      - WEBVIZ_SERVICE_BUS_PAYLOAD_FERNET_KEY
      - APPLICATIONINSIGHTS_CONNECTION_STRING
      - CODESPACE_NAME # Automatically set env. variable by GitHub codespace
    volumes:
      - ./backend_py/primary/primary:/home/appuser/backend_py/primary/primary
      - ./backend_py/libs:/home/appuser/backend_py/libs
    command:
      [
        "sh",
        "-c",
        "pip install debugpy && python -m debugpy --listen 0.0.0.0:5678 -m uvicorn --proxy-headers --host=0.0.0.0 primary.main:app",
      ]

  surface-query:
    build:
      context: .
      dockerfile: ./backend_go/surface_query/Dockerfile.dev
    environment:
      - SURFACE_QUERY_SERVER_MODE=syncserver
    volumes:
      - ./backend_go/surface_query:/home/appuser/backend_go/surface_query

  user-mock:
    build:
      context: .
      dockerfile: ./backend_py/user_mock/Dockerfile
    ports:
      - 8001:8001
    environment:
      - UVICORN_PORT=8001
      - UVICORN_RELOAD=true
    volumes:
      - ./backend_py/user_mock/user_mock:/home/appuser/backend_py/user_mock/user_mock

  user-grid3d-ri:
    build:
      context: .
      dockerfile: ./backend_py/user_grid3d_ri/Dockerfile
    ports:
      - 8002:8002
    environment:
      - UVICORN_PORT=8002
      - UVICORN_RELOAD=true
      - APPLICATIONINSIGHTS_CONNECTION_STRING
    volumes:
      - ./backend_py/user_grid3d_ri/user_grid3d_ri:/home/appuser/backend_py/user_grid3d_ri/user_grid3d_ri
      - ./backend_py/libs:/home/appuser/backend_py/libs

  redis-auth-store:
    build:
      context: .
      dockerfile: docker/redis/Dockerfile
    environment:
      - REDIS_REQUIREPASS=${WEBVIZ_REDIS_AUTH_STORE_PASSWORD}
    expose:
      - 6379
    user: "999:999"
    command: ["--loglevel", "notice"]

  redis-cache:
    build:
      context: .
      dockerfile: docker/redis/Dockerfile
    environment:
      - REDIS_REQUIREPASS=${WEBVIZ_REDIS_CACHE_PASSWORD}
    expose:
      - 6379
    user: "999:999"
    # https://redis.io/docs/latest/operate/oss_and_stack/management/config/#configuring-redis-as-a-cache
    command: ["--maxmemory", "1gb", "--maxmemory-policy", "allkeys-lru", "--loglevel", "notice"]


  pyworker-tables:
    build:
      context: .
      dockerfile: ./backend_py/pyworker_tables/Dockerfile
    environment:
      - AZURE_TENANT_ID
      - AZURE_CLIENT_ID
      - AZURE_CLIENT_SECRET
      - WEBVIZ_SUMO_ENV
      - WEBVIZ_REDIS_CACHE_PASSWORD
      - WEBVIZ_SERVICE_BUS_FQ_NAMESPACE
      - WEBVIZ_SERVICE_BUS_PAYLOAD_FERNET_KEY
      - APPLICATIONINSIGHTS_CONNECTION_STRING
      # For debugging instrumentation
      - OTEL_LOG_LEVEL=debug
      - OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true
    volumes:
      - ./backend_py/pyworker_tables/src:/home/appuser/backend_py/pyworker_tables/src
      - ./backend_py/libs:/home/appuser/backend_py/libs
    command: ["watchfiles", "python -m pyworker_tables.worker_main", "/home/appuser/backend_py/pyworker_tables/src"]
    #command: ["python", "-m", "pyworker_tables.worker_main"]
    #restart: on-failure
    #stop_grace_period: 30s
    restart: always
    # depends_on:
    #   - sb-emulator


# When using the Cosmos DB Emulator, make sure to uncomment the relevant parts in the backend-primary service as well,
# to ensure it waits for the emulator to be healthy before starting.
#
# depends_on:
#   cosmos-db-emulator:
#     condition: service_healthy
#
# Also make sure to comment / uncomment the relevant parts in the backend_py code to use the emulator (main.py).
#   cosmos-db-emulator:
#     image: mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:latest
#     platform: linux/amd64
#     tty: true
#     mem_limit: 4GB
#     ports:
#       - "8081:8081" # HTTPS endpoint
#       - "10250-10255:10250-10255" # Internal emulator ports (needed)
#     environment:
#       - AZURE_COSMOS_EMULATOR_PARTITION_COUNT=1
#       - AZURE_COSMOS_EMULATOR_ENABLE_DATA_PERSISTENCE=true
#     healthcheck:
#       test: curl -k https://127.0.0.1:8081/_explorer/emulator.pem || exit 1
#       interval: 10s
#       retries: 12 # 2 minutes
#       start_period: 30s
#       timeout: 5s
#     volumes:
#       - cosmosdrive:/var/lib/cosmosdb/emulator

# volumes:
#   cosmosdrive:
